<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.liga.waiterservice.mapper.WaiterOrderMapper">

    <resultMap id="WaiterOrderDto" type="ru.liga.waiterservice.model.dto.WaiterOrderDto" autoMapping="true">
    </resultMap>

    <select id="getOrders" resultMap="WaiterOrderDto">
        SELECT order_no, status, create_dttm, waiter_id, table_no
        FROM waiter_order
    </select>
</mapper>
